.idea/
.venv/
# 빌드 산출물 — dist/(기본), dist-pages/(Pages), dist-offline/(릴리즈 zip)
dist/
dist-*/

# 배부용 .docx는 tools/docx/의 생성기가 만든다. 저장소에 담지 않는다.
# 「docx」라는 이름의 폴더는 전부 생성물이라는 것이 이 저장소의 약속이다.
# 생성기를 그냥 돌리면 결과가 여기로 떨어지는데 그건 로컬 확인용이고,
# 배포본은 빌드가 dist/ 안에 새로 만든다.
**/docx/
.github/scripts/node_modules/
