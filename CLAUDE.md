# CLAUDE.md

고등학교 정보 교과 수업용 웹 강의노트와 시뮬레이터 저장소.

**이 문서에는 지켜야 할 규칙만 둔다.** 규칙의 근거·실패 사례·X / O 예시·경위는 사례집에,
절차와 해법은 수정 레시피에, 도구 사용법은 그 스크립트의 머리 주석에 둔다.

| 무엇을 찾는가 | 어디 |
|---|---|
| **지켜야 할 것** | 이 문서 |
| 처음 손대는 사람이 볼 것 (개발 서버·구조·문서 안내) | [`CONTRIBUTING.md`](CONTRIBUTING.md) |
| 그 규칙이 **어떤 실패에서 나왔는지**, X / O 예시 | [`docs/강의노트-작성-사례집.md`](docs/강의노트-작성-사례집.md) |
| 새 차시·새 과목·새 배부 양식을 **추가하는 순서** | [`docs/강의노트-추가하기.md`](docs/강의노트-추가하기.md) |
| 도구 목록(사용법은 각 스크립트 머리 주석) | [`tools/README.md`](tools/README.md) |
| 배부 양식 생성기 | [`tools/docx/README.md`](tools/docx/README.md) |
| 한 번 풀어 본 문제의 해법 | [`docs/수정-레시피.md`](docs/수정-레시피.md) |

## 저장소 구조

| 경로 | 내용 |
|---|---|
| `subjects.json`의 `subjects`에 적힌 과목 폴더 | 강의노트 HTML (`단원번호.제목.html`) |
| `simulator/` | 시뮬레이터. 여러 교과가 함께 쓰는 도구라 **루트에 둔다.** `ai/`는 인공지능기초가, `cs/`는 정보·프로그래밍이 주로 쓴다 |
| `**/code/` | 강의노트가 끌어다 쓰는 `.py`·`.c` 실파일 |
| `**/docx/` | 배부용 보고서 양식이 **빌드 때 생성되는** 자리. 저장소에 담지 않는다 |
| `tools/` | **루트가 부르는 스크립트 전부.** `vite/` 빌드 플러그인 · `checks/` 검사 본체 · `audits/` 감사 · `docx/` 배부 양식 생성기. **전부 Node다** |
| `tests/` | **검사는 Vitest가 돈다.** 검사 하나가 테스트 파일 하나다. `fixtures/`는 일부러 틀리게 쓴 조각 |
| `subjects.json` | **어떤 과목이 있는지 정하는 유일한 곳.** 검사 도구(`tools/lib/repo.mjs`)와 Vite가 함께 읽는다 |
| `vite.config.js` | **빌드 설정은 이 파일 하나다.** 조립만 하고 하는 일은 `tools/vite/`에 있다. CSS 파이프라인도 여기 있다 — 별도 `postcss.config.js`를 두지 않는다 |
| `src/styles/<단위>.css` | 단위별 스타일 진입점. HTML이 이것 하나를 링크한다 |
| `src/entries/<페이지>.js` | **페이지별** 라이브러리 진입점. 그 페이지가 쓰는 것만 담는다 |
| `src/tailwind/<단위>.config.js` | 단위별 Tailwind 설정(`content`를 그 단위로 좁힌다). `base.config.js`는 의존 패키지 CSS가 지나갈 때의 기본값 |
| `privacy/` | 개인정보 처리방침. **폴더 `privacy/`에 파일 `index.html`** 이라야 `…/privacy/` 주소로 열린다 → 아래 규칙 |
| `public/` | **이름이 그대로여야 하는 파일**만. 저장소에 담지 않는다 |
| `.github/` | 배포 워크플로(`workflows/`)와 PR 템플릿 |
| `.idea/runConfigurations/` | IntelliJ 실행 구성(편의용). **전부 `npm run`을 부른다.** `.gitignore`가 `.idea/*`를 막고 이 폴더만 되살린다 |
| `package.json` | **명령을 정의하는 유일한 곳** |
| `CONTRIBUTING.md` | **바깥에서 온 사람이 처음 읽는 문서** |
| `docs/` | 근거(사례집)·절차(추가하기)·해법(수정 레시피) 셋뿐이다 |

---

## 명령은 `package.json`에서만 정의한다

- **스크립트 경로를 직접 치지 않는다.** 새 명령은 `package.json`에 이름을 붙인다.
  검사·감사만 예외로 `tools/run.mjs`의 `CHECKS`·`SIMS`·`AUDITS`에 이름을 붙이고,
  `package.json`은 검사와 감사를 부르는 한 줄씩만 든다.
- **CI도 IDE 실행 구성도 같은 `npm run` 이름만 부른다.** 로컬과 CI는 같은 한 줄 `npm run ci`를 돈다.

| 실행점 | |
|---|---|
| `npm run dev` | Vite 개발 서버 |
| `npm run build` | 배포와 같은 빌드 |
| `npm run ci` | 검사 → 빌드 → 산출물 검사. HTML 규칙 검사가 인자 없이 저장소 전체를 돈다 |

| 조각 | |
|---|---|
| `npm run check` | 검사 전부. 하나가 실패해도 끝까지 돌고 실패한 이름을 모아 보인다 |
| `npm run check -- <이름>…` | 이름을 준 검사만. 이름 뒤의 낱말은 그 검사에 넘긴다(`npm run check -- html <파일>`) |
| `npm run check -- sim [<이름>…]` | 시뮬레이터 동작 검사. 이름 없이 부르면 `tools/run.mjs`의 `SIM_BY_NAME_ONLY`는 빠진다 — **그 시뮬레이터를 고쳤으면 이름으로 손수 돌린다** |
| `npm run audit -- <이름>` | 판정 없이 목록만 내놓는 감사 도구. `ci` 밖이다 |
| `gen:*` · `docx` · `prose` | 생성·추출 도구 |

- **검사 목록은 `tools/run.mjs`의 `CHECKS` 한 곳에만 둔다.** 새 검사는 `tests/<이름>.test.mjs`를 만들고
  거기 한 줄을 더한다. 시뮬레이터 동작 검사는 `SIMS`, 감사 도구는 `AUDITS`다.
  **최상위 이름과 헷갈리는 이름을 만들지 않는다.**
- **검사는 JS(Vitest)로 쓴다. 파이썬 검사를 다시 들이지 않는다.** 파이썬은 강의노트의 `.py`를
  컴파일해 보는 **바깥 도구**로만 부른다 — `gcc`와 같은 자리다.
- **새 규칙을 세우면 `tests/fixtures/`에 걸려야 할 조각을 함께 넣는다.**
- **새 검사를 만들거나 크게 고치면 `tools/mutate.mjs`에 돌연변이를 하나 이상 더하고 `npm run mutate`로
  검사가 우는지 본다.** `ci` 밖이다.
- 전체 목록은 [`tools/README.md`](tools/README.md).

---

## 커밋 규칙

- **커밋 메시지에 AI 협업 표시 문구를 남기지 않는다.** `Co-Authored-By`,
  `🤖 Generated with …`, `coworked`, `Assisted by` 등 도구·협업자 표기 일체 금지.
- 제목은 한국어 한 줄 요약. 본문은 필요할 때만, 무엇을 왜 바꿨는지 항목으로.
- **커밋 메시지는 사용자와 소통하는 창구가 아니다.** `검토 요망`·`판단 요망` 같은
  요청 문구를 쓰지 않는다. 판단이 갈린 지점은 **채팅 응답으로 보고**한다.
- **강의노트는 사용자가 직접 확인하고 넘어간다.** `생성 → 커밋 → 사용자 확인 → 수정 → 다음`.
  - 확인 주기는 과목마다 사용자가 정한다. 기본은 파일 단위, 고등학교 「정보」는 **중단원 단위**.
- **`index.html` 등록은 사용자가 지시할 때만 한다.** 새 과목을 시작해도 **먼저 등록하자고 말하지 않는다.**
- 커밋과 푸시는 사용자가 요청했을 때만 한다.
  - 예외: 「점검 → 수정 → 커밋 → 다음」 같은 반복 워크플로를 지시받았다면
    그 워크플로가 끝날 때까지 파일 단위로 커밋을 이어간다.
- 이 저장소는 `master`에 직접 커밋한다.
- **푸시하기 전에 `npm run ci`를 돌리고, 초록일 때만 푸시한다.** 빨간불이면 고치고 다시 돌린다.
- **커밋은 올릴 파일을 이름으로 골라 `git add`한다.** `git commit -a`를 쓰지 않는다.

---

## 문서와 주석이 낡지 않게 쓰는 법

- **검사 결과를 문서에 적지 않는다**(「위반 0건」·「5개 파일이 걸린다」). 수가 꼭 필요하면 세는 명령을 적는다.
- **사실을 복제하지 말고 그 사실을 지키는 검사를 가리킨다**(「`check -- dist`가 CDN 잔존 0을 보증한다」).
- **「지금 ~이다」꼴 단정문에는 날짜를 붙인다.** 지난 일을 적은 문장은 그대로 두어도 된다.
- **코드가 바뀌면 곧장 어긋나는 것을 가리키지 않는다** — 줄 번호, 형제의 «개수», 「세 번째 규칙」 같은 «차례».
  **자리가 아니라 이름을 가리킨다** — 파일 · 클래스 · 함수 · 속성 이름.
- **도구 사용법은 그 스크립트의 머리 주석 한 곳에만 적는다.** README에는 어떤 파일이 무엇을 하는지만 둔다.

---

## 브라우저 도구는 사용자가 요청할 때만 쓴다

**Claude 브라우저 도구(`mcp__Claude_Browser__*`, `mcp__claude-in-chrome__*`)는
사용자가 요청하지 않았으면 쓰지 않는다.**

- 먼저 `npm run check -- html <파일>`로 확인하고, **스크립트로 확인할 수 없는 것은
  무엇을 확인하지 못했는지 응답에 밝힌다.**
- 실측이 꼭 필요하면 **띄우기 전에 사용자에게 묻는다. 375px 실측을 포함해 예외는 없다.**
  파일을 다 썼다는 이유로 재지 않는다. **재지 않았다고 일이 덜 끝난 것이 아니다** → 「6화면」 절.
- 사용자가 실측을 지시했다면 **대상은 소스가 아니라 `dist/`다.** 재는 법 → [`docs/수정-레시피.md`](docs/수정-레시피.md).
- **`npm run check -- layout`은 이 도구가 아니므로 묻지 않고 돌린다**(headless Chromium, `ci` 안).
  그것이 판정할 수 없는 것(그림이 화면을 넉넉히 쓰는가)은 `tests/browser/shots.test.mjs`로 찍어 눈으로 본다.

---

## HTML 강의안 작성 규칙

- **글자 크기는 최소 `text-base`.** 시뮬레이터의 버튼·표·설명문도 이 규칙 안이다.
  범위 밖은 **그림 «안»의 글자**(SVG·캔버스 라벨)뿐이다. `npm run check -- html`이 본다.
  건너뛸 폴더는 **`subjects.json`의 `font_exempt`에만 적고, 비워 가는 것이 목표다.**
- **테이블은 반드시 `overflow-x-auto` 컨테이너로 감싼다.** 래퍼만으로 부족한 표 → `table-prose`.
- **375px 폭에서 가로 스크롤이 생기지 않아야 한다.**
- **좁은 화면에서 바깥 여백을 줄인다.** `.section-card`는 `padding: clamp(1.25rem, 4vw, 2.5rem)`,
  `<main>`은 `px-4 sm:px-6`. `npm run check -- html`이 둘 다 본다 → 「본문에 남는 폭」.
- **HTML에 CDN 주소를 쓰지 않는다.** 의존성은 전부 npm이고 Vite가 묶는다.
  스타일은 진입점 하나를 링크하고, 라이브러리를 쓰면 진입점 하나를 더 건다.

  ```html
  <link rel="stylesheet" href="/src/styles/<단위>.css">
  <script type="module" src="/src/entries/<페이지>.js"></script>
  ```

  - 새 라이브러리는 **`npm i` 뒤 진입점에서 `import`** 한다. 전역으로 쓰는 것이면 거기서 `window`에 얹는다.
  - **버전은 락파일이 정한다.** 아무 생각 없이 최신을 받지 않는다.
  - **산출물을 둘로 나누지 않는다.** 같은 `dist/`를 Pages와 릴리즈 zip에 똑같이 쓴다.
  - **클래스 이름을 코드로 조립하지 않는다**(`bg-${x}-50`). `npm run check -- classes`가 잡는다.
  - **JS 문자열에 Tailwind 클래스를 적지 않는다.** JS에는 뜻을 가진 이름(`sim-badge`)만 적고
    모양은 `src/styles/_sim-ui.css`에서 `@apply`로 준다. 상태 표시로 켜고 끄는 `hidden`만 예외다.
    같은 검사가 잡는다.
- **밝은 그러데이션 배경(`bg-gradient-to-* from-*-50 to-*-50`)을 쓰지 않는다.** 단색(`bg-sky-50`)으로 준다.
  짙은 그러데이션(흰 글자)은 그대로 두어도 된다. `npm run check -- html`이 잡는다.
- **`npm run dev`로 본다.** 소스 HTML을 파일로 직접 열면 스타일이 붙지 않는다.
- **강의노트끼리 잇는 「이전 차시 · 다음 차시」 바로가기를 만들지 않는다.** 이동은 푸터의 목록 링크로 한다.
- **뒤 차시를 글로 예고하지도 않는다.** 「다음 시간에 배웁니다」·「~을 배운 뒤에 다룹니다」 대신
  **지금 할 수 있는 것만 말한다** — 「지금은 ~까지만 씁니다」. `npm run check -- html`이 잡는다.
- **앞 차시도 가리키지 않는다**(「앞 시간에 배운 대로」). 필요한 개념은 **그 자리에서 한 문장으로 다시 말한다.**
  `npm run check -- prose`가 본다.
  - **다른 강의노트로 거는 링크도 같다**(용어에 건 링크 · 「○○ 다시 보기」). 링크를 빼고 그 자리에서 뜻을 말한다.
    목록(`../index.html`) · 시뮬레이터 · 바깥 주소는 걸어도 된다.
- **같은 파일 «안»에서도 절을 «차례»로 가리키지 않는다**(「세 번째 절」·「마지막 절」).
  이름으로 가리킨다 — 「『규칙과 키』 절에서 봅니다」. 같은 검사가 본다.
- 파일을 고칠 때마다 `npm run check -- html <파일>`. **이 검사가 못 잡는 것**은
  [`tools/checks/html.mjs`](tools/checks/html.mjs) 머리 주석에 있다. 위반을 남기면 `ci`가 빨간불이 된다.

### 섹션 하나는 375px에서 6화면을 넘지 않는다 — 권장이지 필수가 아니다

**6화면은 목표치이지 상한이 아니다. 수업 흐름이 먼저다.** 글을 쓰는 동안 분량을 조절하는 기준이지
재서 통과시켜야 하는 검사가 아니다. 넘겼다는 이유만으로 자르지 않는다.

- 5화면대까지는 괜찮다. **6화면을 넘으면 쪼갤지 검토한다** — 검토이지 결론이 아니다.
- **한 흐름으로 이어지는 내용은 길어도 쪼개지 않는다.** 파일을 나눌지 합칠지도 흐름으로 판단한다.
- **재려고 브라우저를 띄우지 않는다** → 「브라우저 도구는 사용자가 요청할 때만 쓴다」.
  재지 않았으면 재지 않았다고 밝힌다.
- 얹을 것이 크면 **처음부터 독립 `<section>`으로** 만든다.
- 쪼갤 때는 앞 섹션 끝에 **`스스로 확인`을 하나** 둔다.
- nav 항목이 8개를 넘으면 `hidden md:flex`에 `flex-wrap justify-end gap-x-4 gap-y-1`.

### 절을 나눌 때는 래퍼 구조부터 본다

1. **래퍼 하나에 싸여 `<hr>`로 나뉜 구조** — `<hr>` 자리에서 자르고
   **래퍼를 닫았다가 새 절에서 같은 태그를 다시 연다.**
2. **각자 `section-card`인 형제 구조** — 그냥 자르면 된다.

- **정규식으로 `<div>` 스택을 세지 않는다.** 자를 자리의 들여쓰기 깊이를 눈으로 확인한다.
- **자른 직후 반드시 `npm run check -- html <파일>`.**
- 나눈 뒤 **nav 항목을 문서 순서에 맞는 자리에** 넣고, 새 절에는 **`스스로 확인` 밴드**를 하나 둔다.

### 본문에 남는 폭 — 아이콘을 옆에 세우면 좁은 화면에서 글이 토막 난다

- **아이콘 옆에 긴 글을 세우지 않는다.** 세워야 한다면 좁을 때만 쌓는다 — `flex flex-col sm:flex-row`.
- **`items-*`를 반드시 함께 고친다 — `items-stretch sm:items-<원래것>`.** `items-*`가 아예 없으면 그대로 둔다.
  `npm run check -- html`의 「세로로 쌓은 칸」 항목이 본다.
- **한 줄만 고치지 않는다.** 같은 부모 아래 같은 모양의 형제를 함께 바꾼다.
- 짧은 항목 줄은 그대로 둔다.
- 어디가 좁은지는 `npm run audit -- narrow`가 센다(`ci` 밖).

### hero `<h1>`에 `<br>`을 넣지 않는다

제목은 한 줄로 쓰고 줄바꿈은 브라우저에 맡긴다. hero 부제(`<p>`)의 `<br>`은 그대로 쓴다.

### `스스로 확인` 밴드는 `section-card` **바깥**에 둔다

`</section>` 바로 앞, 카드의 **형제**로 놓는다.

```html
        </div>          <!-- section-card 닫기 -->

        <div class="…">…</div>   <!-- 스스로 확인 밴드 -->
    </section>
```

### 사이트 주소는 반드시 하이퍼링크로 단다

```css
a.ext-link { color:#4338ca; font-weight:600; text-decoration:underline;
             text-underline-offset:2px; word-break:break-all; }
a.ext-link:hover { color:#312e81; }
```

- `target="_blank"` + `rel="noopener noreferrer"`를 항상 붙인다. `word-break: break-all`을 빼지 않는다.
- 표시 글자는 `https://www.`를 뺀 짧은 형태, `href`에는 전체 주소.

### 표가 375px에서 눌리는 문제 — `table-prose`

```css
table.table-prose { min-width: 32rem; }
```

- **붙인다** — 셀에 본문 성격의 긴 글이 있거나 **열이 4개 이상**인 표. **붙이지 않는다** — 값이 짧은 2열 표.
- 라벨 열은 `w-28`~`w-44`.
- 표로 제시하는 것이 맞는 내용을 카드로 바꾸지 않는다.
- **flex 자식 안에 넣으면 부모에 `min-w-0`을 함께 준다.** 검사가 잡지 못하므로 넣을 때 함께 준다.

### `<sup>` `<sub>` 태그를 쓰지 않는다

제곱은 `&sup2;` 문자로 쓰고, 아래첨자는 **문장을 고쳐 없앤다.** 검사기는 이것을 잡지 않는다.

### 목록은 `list-outside`만 쓴다 (`list-inside` 금지)

```html
<ol class="space-y-1 text-base font-medium text-slate-800 list-decimal list-outside pl-5">
<ul class="space-y-2 text-base text-slate-700 list-disc list-outside pl-5">
```

- **`pl-5` 이상을 반드시 함께 준다.**
- **학습 목표 번호는 `①②③` 대신 `list-decimal` 자동 번호.**

### 글자가 있는 SVG에는 폭 천장과 가운데 정렬을 함께 준다

```html
<svg class="w-full h-auto max-w-xl mx-auto" viewBox="0 0 520 300" style="min-width:495px">
```

- 천장은 **viewBox 폭에 가까운 `max-w-*`**로 준다. **폭 천장을 주면 `mx-auto`를 반드시 함께 준다.**
- **높이로 묶인 그림(`max-height`, 높이 고정 부모 + `h-full`)에는 정렬을 따로 주지 않는다.**
- `npm run audit -- svg`가 둘 다 본다.
- **SVG를 만들거나 글자 · 좌표를 고쳤으면 `npm run audit -- svg <폴더>`를 돌린다.** 글자 겹침과
  12px 미만을 진짜 브라우저로 잰다. `ci`에 없으므로 **고친 사람이 돌린다.**
- **메모리 도해처럼 새로 그리는 그림은 정해 둔 규격을 쓴다** — viewBox 576 · `max-w-xl` ·
  `min-width:576px` · `font-size` 16 이상 → [`docs/수정-레시피.md`](docs/수정-레시피.md)
- **나란히 놓는 짝은 viewBox 480에 `min-width`와 `max-width`를 둘 다 480으로 준다.**

**바닥선도 함께 준다 — `min-width ≥ viewBox 폭 × 16 ÷ 가장 작은 font-size`.**

- **모자라면 `font-size`가 아니라 그리는 폭을 올린다.**
- 폭을 올리기 전에 **`overflow-x-auto` 안인지 확인한다.**
- 가장 작은 `font-size`는 `<text>`뿐 아니라 **`<g>`에 붙어 상속되는 것까지** 본다.
- `npm run check -- html`이 12px 미만은 위반, 12~16px는 「확인 필요」로 잡는다.

### 「차트 예시」는 도형만 그려도 되지만 「도식」은 이름이 안에 있어야 한다

- **차트 예시**(막대·선·산점도·히트맵)는 도형만 그리고 축·범례·출처를 HTML로 붙인다.
- **도식**(흐름도·구성도·해부도·관계도)은 **이름을 그림 안에** 넣는다.
- **밖에 「왼쪽 ○○ · 가운데 △△」 같은 위치 해독표를 적지 않는다.** 보이면 이름을 안으로 옮기고
  **밖의 해독표를 지운다.**

### 코드 요소의 한글 글꼴

요소 선택자에 `body`를 붙여 특정도를 올린다.

```css
body code, body kbd, body samp, body pre,
body .font-mono { font-family: ui-monospace, …, "Pretendard Variable", …, monospace; }
```

### 코드 블록 가로 넘침

`<pre>`를 쓰는 모든 파일에 넣는다.

```css
body pre { overflow-x: auto; max-width: 100%; min-width: 0; }
```

- **`white-space: pre-wrap`으로 접지 않는다.**
- 래퍼 `div`를 끼우지 않는다.

### 코드 자체는 HTML에 쓰지 않는다

`.py`·`.c`는 **강의노트 옆 `code/`에 실파일로** 두고, HTML에는 마커만 둔다.
문법 → [`tools/vite/inject-code.js`](tools/vite/inject-code.js) 머리 주석.

```html
<pre><code class="language-c" data-src="code/1-2.조건.c#비교"></code></pre>
```

- **짧은 조각도 파일로 뺀다.** 소스 파일에는 코드를 이스케이프하지 않고 그냥 코드로 쓴다.
- **복사 버튼은 적지 않는다.** 빌드가 붙인다 → `tools/vite/copy-code-button.js`
- **코드 블록이 있는 페이지는 Prism 진입점을 건다.** 진입점은 쓰는 언어의 `_lib/prism-<언어>.js`를 받고
  `Prism.highlightAll()`을 부른다.
- `npm run check -- code`가 구문 오류 · `<pre><code>`에 직접 적은 코드 · Prism 진입점을 본다. 마커가 가리키는 파일 · 구역이 없으면 빌드(`npm run build`)가 선다.
  `<code>` 없는 `<pre>`(표·주소 같은 고정폭 글)는 코드가 아니므로 둔다.

#### 코드 파일 이름 — 앞에 강의노트를 적고, 띄어 쓰지 않는다

```
프로그래밍/기초/code/1-2.인사.c              <- 1-2 강의노트가 쓰는 파일
프로그래밍/실습/code/비만도.표준몸무게.py     <- 번호가 없는 실습은 알아볼 이름을 쓴다
```

- **앞에 그 코드를 쓰는 강의노트를 적고 `.`으로 잇는다.** 차시 번호가 있으면 번호를,
  번호가 없는 실습(**R1**)은 알아볼 수 있는 이름을 쓴다.
- **공백을 쓰지 않는다.** 띄어 쓸 자리는 `-`로 잇는다.
- 이름을 바꾸면 **그 파일을 가리키던 `data-src`도 함께 고친다.**
- `npm run check -- code`는 **공백만 본다.** 앞머리는 사람이 본다.

### 용어의 한국어·영어 표기는 한 줄에 붙인다

```html
<p class="font-black text-lg text-sky-900">상관관계(correlation)</p>   <!-- O -->
```

핵심 정리 카드의 `<h3>` + 부제 패턴은 예외다.

### 짝을 이루는 개념은 나란히 배치한다

회귀↔분류처럼 **학생이 자주 뒤섞는 짝**은 `grid grid-cols-1 lg:grid-cols-2`로 놓는다.

- 두 그림은 **같은 viewBox·같은 축**으로 그리고, 소제목도 대구로 맞춘다.
- 나란히 놓은 뒤 **차이를 못박는 상자**를 하나 둔다.

### 예시 데이터의 인물은 반드시 익명화한다

사람을 가리키는 예시 값(학생·교사·환자 등)은 **`학생 A`, `학생 B`**(`환자 A`)로 쓴다.
학번·점수·날짜처럼 **사람을 특정하지 않는 값은 원자료 그대로** 둔다.

### 문체 기준서

전 과목 강의노트의 문체다. X / O 예 → [사례집](docs/강의노트-작성-사례집.md) 「문체 기준서」.
`npm run check -- prose`가 지킨다.

- **기계로 고칠 수 있는 것**(2 · 3 · 12 · 15)은 곧바로 막고, 시뮬레이터 화면 글에도 건다(15 제외).
- **사람이 읽고 고칠 것**(4 · 5 · 6 · 7 · 8 · 9 · 11)은 `STYLE_DONE`에 올린 과목부터 막는다.
  새 과목은 정제하는 동안 빠져 있고, `npm run check -- prose --report`가 남은 자리를 센다.
- **시뮬레이터 페이지도 강의노트와 같이 본다** — 4~9 · 11과 15를 `SIM_STYLE_DONE`에 올린 폴더부터 막는다.
  예외는 둘이다 — 화면의 이모지(12)와 시뮬레이터끼리 거는 링크(`simulator/index.html`의 목록 포함).
- 1 · 10 · 13 · 14는 검사가 없다 — 쓸 때 사람이 본다.

1. **본문·퀴즈 해설은 합니다체, 핵심 정리 카드·표 셀은 해라체.** 한 자리에서 섞지 않는다.
2. **정답 해설은 감탄사 없이 첫 문장에서 이유를 말한다.** 「맞습니다!」「정확합니다!」「정답!」을
   쓰지 않는다. 오답 해설은 「맞습니다」「맞는 설명입니다」처럼 옳다는 말로 열지 않는다.
   첫 문장이 그 선택지의 내용이나 까닭을 말한다.
3. **인용·강조 부호는 「 」 하나.** ‘ ’ “ ” « »와 학생 글 속의 곧은따옴표(' ' " ")를 쓰지 않는다.
   코드(`<code>`·`<pre>`, 코드의 문자열)는 그대로 둔다. 영어 원어는 괄호에.
4. **줄표(—)는 한 문단에 한 번까지.** 접속사 대신 쓰지 않는다(「당연합니다 — …이니까요」).
5. **볼드는 한 문단에 한 구절, 한 절에 세 곳 이내.** 정의 낱말과 구분하는 물음에만 주고,
   조사·어미는 볼드 밖에 둔다. **본문 문단만 센다** — 퀴즈·표·카드·상자·「스스로 확인」 안은 뺀다.
6. **「자리」는 위치의 뜻으로만 쓴다.** 단계·역할·대목이면 그 낱말로. 「이 자리의 · 바로 이 자리 ·
   자리가 바로」를 쓰지 않는다.
7. **판정문으로 문단을 닫지 않는다.** 「~이 요점/핵심/열쇠/요령입니다」「그것이 ~한 점입니다」 대신
   요점을 첫 문장에 둔다.
8. **명사 셋 이상을 늘어놓고 「~등의 …을 분석하여 …합니다」로 닫지 않는다.** 구체적 예 하나로 쓴다.
9. **「스스로 확인」에 되읽기 지시를 두지 않는다.** 「다시 읽어 보세요」「가리고 적어 보세요」 대신
   본문에 없는 새 상황 하나를 준다.
10. **정의는 한 곳에만 둔다.** hero·핵심 정리·퀴즈 해설에 정의 문장을 복제하지 않는다.
    핵심 정리는 구별점·조건·한계를 적는다.
11. **「AI와 함께 정리하기」는 「핵심 정리」에 흡수하고 지운다.** 한 파일에 정리 상자는 하나다.
12. **이모지 · 「VS」「CHECK-UP」 · 해시태그 · 「※」「예)」「예:」「핵심 해석:」 같은 라벨을 쓰지 않는다.**
    예는 문장으로 든다(「~이 그렇습니다」). 괄호 속 「(예: …)」는 덧붙임이므로 둔다.
    상자 이름은 「헷갈리기 쉬운 점」「더 알아보기」「스스로 확인」「스스로 점검하기」(절 단위 점검)
    「해 보기」에서 고른다. 상자 이름으로 쓴 배지는 그 절의 제목과 같아야 한다.
    **시뮬레이터 화면의 이모지는 예외로 지우지 않는다(사용자 확정).**
13. **예시 데이터 값에는 출처 또는 「수업에서 원리를 확인하려고 지어낸 값」을 단다.**
    지어낸 값을 출처처럼 보이는 표현으로 감싸지 않는다.
14. **본문이 가리키는 것은 본문에 글로 있어야 한다.** 「위의 세 규칙」이 그림·퀴즈에만 있으면 안 된다.
15. **퀴즈 머리말은 「확인 퀴즈」 · 「배운 내용을 확인해 봅시다.」로 통일한다.** 느낌표를 쓰지 않는다.
    정한 문구는 `QUIZ_TITLE` · `QUIZ_LEAD`에 있다. 부제가 없는 퀴즈도 그 파일의 제목 줄 모양대로 부제를 넣는다.

---

## 참고 자료는 TXT와 PDF를 함께 읽는다

**TXT만 읽고 쓰지 않는다.**

1. **TXT로 용어와 수치를 확보**한다.
2. **같은 범위의 PDF 쪽을 이미지로 연다.** 그림이 무엇을 보여 주려 했는지 확인한다.
3. **의도만 가져온다.** 설명 순서 · 비유 · 그래픽은 **전부 새로 짠다.**
   개념 설명의 흐름을 뒤집는 것이 학생에게 더 낫다면 뒤집는다.

쪽 오프셋을 먼저 확정하고 적어 둔다. 확정된 값 —
**고등학교 「정보」 지도서(수업편) 1~5권 = Ⅰ~Ⅴ단원, `PDF쪽 = 교과서쪽 − 7`.**

## 시뮬레이터는 조작의 복잡도로 위치를 정한다

- **조작이 간단하면 강의노트 안에 내장**한다.
- **기능이 많거나 시뮬레이션이 복잡하면 별도 페이지**(`simulator/<ai|cs>/…`)로 뺀다.
  다만 **대단원이 끝날 때 사용자에게 보고하고 나서 제작**한다. 먼저 만들지 않는다.
- **시뮬레이터는 어느 교과에도 딸리지 않는다.** 만든 맥락으로 `ai/`·`cs/`에 넣되,
  쓰는 교과가 여럿이면 **파일을 복제하지 말고 `index.html`에서 여러 번 링크한다.**
- **강의노트에서 교과를 넘어 걸어도 된다. 다만 관련 있어 보인다고 걸지 않는다** — 글로는 보여 줄 수
  없는 것을 학생에게 상상하라고 시키는 곳에만 둔다. 방금 갈라 놓은 개념을 도로 섞는 링크
  (회귀를 말하는 절에 분류판 시뮬레이터)는 넣지 않는다.
- **캔버스를 쓰면 `window.fitCanvas`로 잰다.** 직접 재지 않는다 → [`docs/수정-레시피.md`](docs/수정-레시피.md).

### 시뮬레이터의 입구(`simulator/index.html`)는 손으로 쓰지 않는다

- **`simulator/index.html`을 손으로 고치지 않는다.** 루트 `index.html`을 고친 뒤 `npm run gen:sim-index`를 돌린다.
- **시뮬레이터 목록은 루트 `index.html`에만 둔다.**
- **새 시뮬레이터를 만들면 루트 `index.html`에 링크를 건다.** 등록을 미룰 것이면 `tools/checks/sim-index.mjs`의
  `UNLINKED_OK`에 **왜 미루는지와 함께** 적는다. `npm run check`가 양쪽을 본다.
- 루트를 크게 손봐 굽는 규칙이 자리를 못 찾으면 **규칙도 함께 고친다.**

### 전체 화면은 그것만 켜고 수업할 수 있어야 한다

**전체 화면에서 못 누르는 조작은 없는 것과 같다.**

- **무대(`fs-stage`)는 그림과 재생 줄뿐 아니라 조작까지 감싼다**(탭 줄은 예외 — 「탭 줄만은」 항목). 읽는 카드(화면 읽는 법 · 눈여겨볼 것 · 비용표)는
  무대 **밖**에 둔다.
- **조작에 `fs-hide`를 주지 않는다.** 세로가 빠듯하면 줄일 것은 설명이다.
- **넓은 화면에서 폭을 쓴다.** 가로로 긴 그림은 위아래로, 세로로 큰 그림은 좌우로 나눈다. 등록부의 `shape`에 적는다.
- **탭 줄만은 전체 화면 밖에 남긴다(사용자 확정).** 탭은 조작이 아니라 켜기 «전»에 무대를 고르는 것이다.
- 규약(`fs-cols` · `fs-main` · `fs-side` · `fs-tabs` · `fs-outside` · `sim-deck`)은
  [`src/styles/simulator.css`](src/styles/simulator.css) 머리에 있다.
- **누를 때마다 화면이 흔들리지 않아야 한다.** 단계마다 길이가 바뀌는 설명은 높이를 못박고,
  연산을 눌러도 재생하지 않는다 — 재생은 따로 누른다. `sim-deck` 무대는 조작이 제 높이만 갖고
  남는 세로를 설명과 그림이 갖는다. `npm run check -- layout`이 흔들림을 본다.
- **CSS는 `:fullscreen`이 아니라 `fs-on` 클래스를 보게 한다.** `npm run check -- sim fullscreen`이 지킨다.

### 시뮬레이터의 말도 교과 용어로 쓴다

- **정식 용어를 풀어 쓰지 않는다.** 「찾기 위한 트리」가 아니라 **탐색 트리**,
  「마디」가 아니라 **노드**, 「나무」가 아니라 **트리**, 「훑는다」가 아니라
  **순차 탐색한다 · 순회한다**.
  - **「훑다」는 강의노트에서도 쓰지 않는다.** 사람이 눈으로 보는 것은 「살피다」로 쓴다.
    `tools/checks/html.mjs`의 `BANNED_WORDS`가 모든 HTML(`check -- html`)과 시뮬레이터 JS(`check -- terms`)에서 지킨다.
  - **낱말이 이름 자리(절 제목 · nav · 학습 목표 · 짝을 이루는 이름)에도 있으면 파일을 통째로 바꾸거나
    그대로 둔다.** 절 제목을 바꾸면 **`data-src`가 가리키는 코드 파일 이름까지** 바꾼다
    → [`docs/수정-레시피.md`](docs/수정-레시피.md)
- **비용을 「값」이라 적지 않는다.** **비용**으로만 적는다.
- 화면에 나오는 «순서»는 **순서**, 조작하는 것은 **버튼**, 한 번의 실행은 **회차**다.
- `npm run check -- terms`가 본다. 용어를 하나 정하면 `tools/checks/terms.mjs`의 `SIM_TERMS`에 넣는다.

### 동작의 이름은 한자어로 쓴다

**누르는 것의 이름**(버튼 · 고르개 항목 · 접는 상자의 머리)은 한자어다 —
**삭제 · 제거 · 해제 · 선택 · 추가 · 추출 · 분할 · 교체 · 이동 · 조정**이지
「지우기 · 떼기 · 고르기 · 담기 · 뽑기 · 나누기 · 바꾸기 · 옮기기」가 아니다.

- **순우리말은 닫힌 허용 목록에 있는 것만 쓴다.** 목록에 없으면 한자어를 찾는다 —
  **굳은 말인지 판단하지 않는다.** 목록도 물러난 말도 `tools/checks/verbs.mjs`가 들고 있다.
- **없애는 일은 셋으로 가른다** — 자료가 실제로 없어지면 **삭제**, 목록에서 빠질 뿐 자료는 그대로면
  **제거**, 설정이나 지정을 풀면 **해제**.
- **바꾸는 것도 가른다** — 이름은 **변경**, 있던 것을 밀어내고 다른 것을 넣으면 **교체**, 값을 옮기는 것이면 **조정**.
- **문장의 일반 동사는 그대로 둔다**(「씁니다 · 봅니다 · 나옵니다 · 켜다 · 끄다」, 설명문의 비유).
  바꾸는 것은 **누르는 것의 이름과 그것을 가리키는 서술**까지다.
- **어색한 순우리말 동사는 이름이 아닌 문장에서도 쓰지 않는다** — 「펴다 · 펼치다」가 그렇다. <!-- verbs: 예시 -->
- `npm run check -- verbs`가 본다. 목록에는 **활용형을 손으로 적고, 그 낱말의 `-ㄹ`/`-ㅂ` 꼴부터 적는다.**

### 강의노트 문장은 정제에서 물러난 말을 다시 쓰지 않는다

**강의노트 문장의 순우리말 동사는 꼭 필요한 곳이 아니면 한자어 동사로 쓴다.** 생성된 글의 틀
(격언투 맺음 · 「라벨 —」 머리말 · 같은 어원 풀이의 반복)도 쓰지 않는다.

- **정제에서 한 번 물러난 말은 `tools/checks/prose.mjs`의 목록에 올린다.** 다음 과목은 읽기 전에 그 목록부터 돌린다.
- 목록의 줄마다 **걸려야 할 꼴(`예`)과 걸리면 안 되는 꼴(`아님`)**을 적는다.
- **정제를 마친 파일만 막는다(`DONE`).** 새 과목은 정제하는 동안 빠져 있고, `--report`가 걸린 곳을 센다.
  사용자 확인이 끝난 파일을 올리고, 한 번 올린 파일은 되돌리지 않는다.
- **시뮬레이터도 같은 목록으로 막는다.** 범위는 `check -- terms`와 같은 import 그래프이고,
  JS는 문자열 리터럴 속만 본다.
- `npm run check -- prose`가 본다. `ci`에 들어 있다.

## 개인정보 처리방침은 코드에 매여 있다

`privacy/index.html`은 **바깥에 내놓는 정본이다.**

- **조 번호를 다시 매기지 않는다.** 더할 것은 뒤에 붙이고, 뺄 것은 그 조에 「삭제」라고 적는다.
- **방침이 못박은 것 넷은 코드가 지킨다** — 브라우저 저장소를 쓰지 않는다(제4조) · 바깥으로
  나가는 요청은 밝힌 것뿐이다(제7조) · 카메라를 쓰는 시뮬레이터는 하나뿐이다(제8조) ·
  첫 화면과 시뮬레이터 입구에서 방침으로 가는 길이 있다. `npm run check -- privacy`가 본다.
- **거기서 빨간불이 나면 그 코드를 되돌리거나, 방침을 고치고 시행일을 새로 적는다.**
  검사를 느슨하게 풀지 않는다.

## 강의노트는 자체 서술이 원칙이다

강의노트는 **웹에 공개되는 독립 저작물**이다. **참고 자료를 갖고 있지 않아도 온전히 읽히도록** 쓴다.

1. **참고 자료 내부의 번호(`[표 Ⅱ-9]`)를 캡션에 쓰지 않는다.** 캡션은 **무엇을 담은 표인지**로 쓴다.
   HTML 주석에도 남기지 않는다.
2. **데이터는 원출처를 밝힌다.** 표·그림 캡션에 함께 적는다. 출처가 없는 값이면
   「[수업용 자료]」나 「수업에서 원리를 확인하려고 지어낸 값」이라고 밝힌다.
3. **설명 문장은 직접 쓴다.** 개념 정의를 옮겨 적지 않는다. `fa-quote-left` 인용 블록은
   **실존 인물의 발언에만** 쓰고 누가 한 말인지 밝힌다.
4. 용어의 한국어·영어 표기, 분류 체계, 값의 범위 같은 **사실은 그대로** 쓴다.

---

## 오개념을 심지 않는 서술

**파일을 쓸 때마다 아래 목록을 확인한다. 7 · 4 · 8번을 먼저 본다.**
근거와 X / O 예시는 [사례집 2장](docs/강의노트-작성-사례집.md)에 있다.

1. **관찰한 데이터의 기울기를 인과 화법으로 읽지 않는다.** **비교 화법**으로 쓴다
   (직접 조작한 실험이라면 인과 화법이 정당하다). 독립·종속 변수를 지정하는 두 문장의 화법을 맞춘다.
   **집단 단위의 관계를 개인에게 옮기지 않는다.**
2. **절편이 음수이거나 x=0이 현실에 없으면 반드시 짚는다.** 절편을 「기본값」이라 정의하지 않고
   **선의 높이를 맞춰 주는 숫자**, **데이터가 없는 구간까지 선을 늘였을 때 나오는 값**으로 설명한다.
3. **정의가 순환하지 않게 쓴다.** 나란히 놓이는 개념은 **구별점을 정의문 안에** 넣는다.
   구별을 보조 박스로 미루지 않는다.
4. **용어를 무주석으로 처음 등장시키지 않는다.** 처음 나오는 말은 **그 자리에서** 괄호로 뜻을 단다.
   뒤에서 설명한다면 그렇다고 밝힌다. 다시 나오지 않을 약어는 지우거나 「이름까지 외울 필요는 없다」를 붙인다.
   **같은 개념을 두 파일에서 다르게 말하지 않는다**(새 파일을 쓸 때 앞 파일의 정의를 먼저 본다).
   **같은 낱말을 두 뜻으로 쓸 때는 그 자리에서 이름 충돌을 밝힌다.**
5. **정의문이 뒤에 나올 예시를 배제하지 않는지 확인한다.** 요건과 **흔한 형태**를 섞지 않는다.
   정의를 고치면 **핵심 정리 카드와 퀴즈 해설의 같은 문장도** 함께 고친다.
6. **파일을 잇는 예시 데이터는 값과 이름까지 동결한다.** 값을 바꿔야 한다면 잇는다는 표현을 빼고 새 상황으로 제시한다.
7. **조건이 붙는 사실을 무조건으로 쓰지 않는다.** 방법 하나만 옳은 것처럼 쓰지 않는다.
   새 파일을 쓴 뒤 **도입부의 단정 문장과 실습·결론을 나란히 놓고 읽는다.**
   **앞 파일이 조건부로 세운 것을 뒤 파일이 무조건으로 되돌리지 않는다.**
8. **절 제목·표 셀·핵심 정리 카드의 압축 문장이 본문보다 단정해지지 않게 한다.**
   절 제목이 결론을 미리 정해 주지 않는다. **파일을 다 쓴 뒤 제목·카드·표 셀·퀴즈 해설만 따로 읽어 본문과 대조한다.**
9. **출처를 댈 수 없는 수치와 기업·제품 실명을 쓰지 않는다. 예측을 사실처럼 적지 않는다.**
   연도·주체를 확실히 댈 수 없으면 일반화해서 쓴다. 특정 기업을 비난하는 글이 되지 않게 한다.
   값이 필요한 표·그림에는 **「수업에서 원리를 확인하려고 지어낸 값」**이라고 밝힌다.
10. **딜레마와 논쟁에 정답을 정해 주지 않는다. 「정답이 없다」로 끝내지도 않는다.**
    **각 선택이 무엇을 지키고 무엇을 내려놓는지**를 나란히 놓는다. **많이 골랐다는 것과 옳다는 것을 구별한다.**
    원칙을 나열만 하지 않고 부딪히는 짝을 하나라도 보인다. **해결 방안에는 남는 어려움을 함께 적는다.**
11. **본문을 고치면 학습 목표·nav·핵심 정리·퀴즈 해설의 낱말도 함께 본다.** 반대로 목표에 있는 말이
    본문에 아예 없는 경우도 본다.
