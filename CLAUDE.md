# CLAUDE.md

고등학교 정보·인공지능 수업용 웹 강의안과 시뮬레이터 저장소.

**이 문서에는 규칙만 둔다.** 근거·사례·절차·해법은 전부 `docs/`에 있다.
규칙을 고칠 때 근거까지 여기 적고 싶어지면, 그 근거는 사례집으로 보낸다.

| 무엇을 찾는가 | 어디 |
|---|---|
| **지켜야 할 것** | 이 문서 |
| 그 규칙이 **어떤 실패에서 나왔는지**, X / O 예시 | [`docs/강의노트-작성-사례집.md`](docs/강의노트-작성-사례집.md) |
| 새 차시·새 과목·새 배부 양식을 **추가하는 순서** | [`docs/강의노트-추가하기.md`](docs/강의노트-추가하기.md) |
| 도구 사용법, 미리보기, 코드 주입 | [`tools/README.md`](tools/README.md) |
| 배부 양식 생성기 | [`tools/docx/README.md`](tools/docx/README.md) |
| 시뮬레이터 검증 절차와 루브릭 | [`docs/시뮬레이터-검증-워크플로.md`](docs/시뮬레이터-검증-워크플로.md) |
| 한 번 풀어 본 문제의 해법 | [`docs/수정-레시피.md`](docs/수정-레시피.md) |

## 저장소 구조

| 경로 | 내용 |
|---|---|
| `인공지능기초/` · `데이터과학/` · `정보(고등학교)/` · `프로그래밍(Python)/` · `프로그래밍(C)/` | 강의노트 HTML (`단원번호.제목.html`) |
| `simulator/ai/` | AI 시뮬레이터 18종 |
| `**/code/` | 강의노트가 끌어다 쓰는 `.py`·`.c` 실파일 |
| `**/docx/` | 배부용 보고서 양식이 **빌드 때 생성되는** 자리. 저장소에 담지 않는다 |
| `dist/templates/py`·`c` | 프로그래밍 배부 양식이 나가는 **옛 자리.** 소스에는 없고 빌드만 만든다. 강의노트 15개가 이 URL을 링크한다 — C·Python 통합 때 위 규칙으로 옮긴다 |
| `tools/` | 점검 스크립트와 배부 양식 생성기 |
| `subjects.json` | **어떤 과목이 있는지 정하는 유일한 곳.** 파이썬과 Vite가 함께 읽는다 |
| `vite.config.js` | 빌드 조립기. 하는 일은 `build/`에 나뉘어 있다 |
| `build/` | Vite 빌드 플러그인. 하는 일마다 파일 하나 |
| `src/styles/<단위>.css` | 단위별 스타일 진입점. HTML이 이것 하나를 링크한다 |
| `src/entries/<페이지>.js` | **페이지별** 라이브러리 진입점. 그 페이지가 쓰는 것만 담는다 |
| `tailwind/<단위>.config.js` | 단위별 Tailwind 설정(`content`를 그 단위로 좁힌다) |
| `public/` | **이름이 그대로여야 하는 파일**만. 지금은 MathJax 글꼴뿐. 저장소에 담지 않는다 |
| `.github/workflows/` | 배포 워크플로 |
| `.idea/runConfigurations/` | IntelliJ 실행 구성. **전부 `npm run`을 부른다.** `.gitignore`가 `.idea/*`를 막고 이 폴더만 되살린다 |
| `package.json` | **명령을 정의하는 유일한 곳** |
| `docs/` | 근거·절차·리포트 |

---

## 명령은 `package.json`에서만 정의한다

**스크립트 경로를 직접 치지 않는다.** 새 명령이 필요하면 `package.json`에 이름을 붙인다.
**CI도 IDE 실행 구성도 같은 이름을 부른다** — 여기만 고치면 셋이 함께 바뀐다.

**실행점은 셋뿐이다.** IntelliJ 실행 구성도 GitHub Actions도 이것만 부른다.

| | |
|---|---|
| `npm run dev` | Vite 개발 서버 |
| `npm run build` | 배포와 같은 빌드 |
| `npm run ci` | 검사 → 빌드 → 산출물 검사. **HTML 규칙 검사가 여기 들어 있다** — 인자 없이 저장소 전체를 돈다 |

나머지는 이 셋이 조립해 쓰는 조각이다. 인자는 `--` 뒤에 넘긴다
(`npm run check:html -- <파일>`). 전체 목록은 [`tools/README.md`](tools/README.md).

---

## 커밋 규칙

- **커밋 메시지에 AI 협업 표시 문구를 남기지 않는다.** `Co-Authored-By`,
  `🤖 Generated with …`, `coworked`, `Assisted by` 등 도구·협업자 표기 일체 금지.
- 제목은 한국어 한 줄 요약. 본문은 필요할 때만, 무엇을 왜 바꿨는지 항목으로.
- **커밋 메시지는 사용자와 소통하는 창구가 아니다.** `검토 요망`·`판단 요망` 같은
  요청 문구를 쓰지 않는다. 판단이 갈린 지점은 **채팅 응답으로 보고**한다.
- **강의노트는 사용자가 직접 확인하고 넘어간다.**
  `생성 → 커밋 → 사용자 확인 → 수정 → 다음` 순서를 지킨다.
  - **확인 주기는 과목마다 사용자가 정한다.** 기본은 파일 단위.
    고등학교 「정보」는 **중단원 단위**(2026-08-11 사용자 확정).
- **`index.html` 등록은 사용자가 지시할 때만 한다.** 새 과목을 시작해도
  **먼저 등록하자고 말하지 않는다**(2026-08-11 사용자 확정).
- 커밋과 푸시는 사용자가 요청했을 때만 한다.
  - 예외: 「점검 → 수정 → 커밋 → 다음」 같은 반복 워크플로를 지시받았다면
    그 워크플로가 끝날 때까지 파일 단위로 커밋을 이어간다.
- 이 저장소는 `master`에 직접 커밋한다.

---

## 브라우저 도구는 사용자가 요청할 때만 쓴다

**Claude 브라우저 도구(`mcp__Claude_Browser__*`, `mcp__claude-in-chrome__*`)를
사용자가 요청하지 않았으면 쓰지 않는다.** 토큰을 많이 먹고 오래 걸린다.

- 먼저 `npm run check:html -- <파일>`로 확인하고, **스크립트로 확인할 수 없는 것은
  무엇을 확인하지 못했는지 응답에 밝힌다.**
- 실측이 꼭 필요하면 **띄우기 전에 사용자에게 묻는다.**
  - **예외 — 강의노트 375px 실측은 파일당 1회 상시 허용**(2026-08-11 사용자 확정).
    파일을 다 쓴 뒤 한 번만 재고 보고한다. 파일당 1회를 넘겨 반복하지 않는다.
  - **실측 대상은 소스가 아니라 `dist/`다.** 재는 법 → [`docs/수정-레시피.md`](docs/수정-레시피.md).

---

## HTML 강의안 작성 규칙

- **글자 크기는 최소 `text-base`.** 교실 뒷자리·빔프로젝터 기준이다.
  시뮬레이터의 조작 UI는 이 규칙의 범위 밖이다.
- **테이블은 반드시 `overflow-x-auto` 컨테이너로 감싼다.** 래퍼만으로는 부족하다 → `table-prose`.
- **375px 폭에서 가로 스크롤이 생기지 않아야 한다.**
- **HTML에 CDN 주소를 쓰지 않는다.** 의존성은 전부 npm이고 Vite가 묶는다.
  스타일은 진입점 하나를 링크하고, 라이브러리를 쓰면 진입점 하나를 더 건다.

  ```html
  <link rel="stylesheet" href="/src/styles/<단위>.css">
  <script type="module" src="/src/entries/<페이지>.js"></script>
  ```

  - 새 라이브러리는 **`npm i` 뒤 진입점에서 `import`** 한다. 전역으로 쓰는 것이면
    거기서 `window`에 얹는다. 절차 → [`tools/README.md`](tools/README.md)
  - **버전은 락파일이 정한다.** 아무 생각 없이 최신을 받지 않는다 —
    Font Awesome은 7에서 아이콘 이름이 바뀌고 p5는 2에서 호환이 깨진다.
  - **산출물을 둘로 나누지 않는다.** 같은 `dist/`를 Pages와 릴리즈 zip에 똑같이 쓴다.
  - **클래스 이름을 코드로 조립하지 않는다.** 빌드가 리터럴로 있는 클래스만 굽는다.
    `bg-${x}-50`은 CSS에서 빠지는데 dev에서는 멀쩡히 보인다. `npm run check:classes`가 잡는다.
- **`npm run dev`로 본다.** 소스 HTML을 파일로 직접 열면 스타일이 붙지 않는다.
- **강의노트끼리 잇는 「이전 차시 · 다음 차시」 바로가기를 만들지 않는다.**
  이동은 푸터의 목록 링크로 한다. 본문에서 **글로 예고하는 것은 괜찮다.**
- 파일을 고칠 때마다 `npm run check:html -- <파일>`.
  **이 검사가 못 잡는 것이 있다** → [`tools/README.md`](tools/README.md).
  인자 없이 돌리면 저장소 전체를 보고, `npm run ci`가 그렇게 부른다 —
  **그러니 위반을 남긴 채로 두면 CI가 빨간불이 된다.**

### 섹션 하나는 375px에서 6화면을 넘지 않는다 — 권장이지 필수가 아니다

**6화면은 목표치이지 상한이 아니다**(2026-08-11 사용자 확정). **수업 흐름이 먼저다.**

- **5화면대까지는 괜찮다. 6화면을 넘으면 쪼갤지 검토한다.**
- **한 흐름으로 이어지는 내용은 길어도 쪼개지 않는다.**
  같은 이유로 **파일을 나눌지 합칠지도 흐름으로 판단한다** — 분량만으로 쪼개지 않는다.
- 초과했다면 고치지 않더라도 **몇 화면인지 응답에 보고**한다. 판단은 사용자가 한다.
- **내용을 채우면 거의 반드시 넘긴다. 재기 전에는 끝난 것이 아니다.**
  얹을 것이 크면 **처음부터 독립 `<section>`으로** 만든다.
- 쪼갤 때는 앞 섹션 끝에 **`스스로 확인`을 하나 두어** 멈춰서 생각할 거리를 준다.
- nav 항목이 8개를 넘으면 `hidden md:flex`에 `flex-wrap justify-end gap-x-4 gap-y-1`.

### 절을 나눌 때는 래퍼 구조부터 본다

섹션은 두 형태뿐이다. **어느 쪽인지 보지 않고 자르면 중첩이 깨진다.**

1. **래퍼 하나에 싸여 `<hr>`로 나뉜 구조** — `<hr>` 자리에서 자르고
   **래퍼를 닫았다가 새 절에서 같은 태그를 다시 연다.**
2. **각자 `section-card`인 형제 구조** — 그냥 자르면 된다.

- **정규식으로 `<div>` 스택을 세지 마라.** **자를 자리의 들여쓰기 깊이를 눈으로 확인**한다.
- **자른 직후 반드시 `npm run check:html -- <파일>`.**
- 나눈 뒤 **nav 항목을 문서 순서에 맞는 자리에** 넣는다.
- 새 절에는 **`스스로 확인` 밴드**를 하나 둔다.

### hero `<h1>`에 `<br>`을 넣지 않는다

제목은 한 줄로 쓰고 줄바꿈은 브라우저에 맡긴다(2026-08-11 사용자 확정).
hero 부제(`<p>`)의 `<br>`은 그대로 쓴다.

```html
<h1 class="text-4xl md:text-6xl font-black mb-6 leading-tight">
    유선 네트워크와 무선 네트워크
</h1>
```

### `스스로 확인` 밴드는 `section-card` **바깥**에 둔다

`</section>` 바로 앞, 카드의 **형제**로 놓는다.

```html
        </div>          <!-- section-card 닫기 -->

        <div class="bg-gradient-to-r from-sky-50 to-slate-50 …">…</div>
    </section>
```

### 사이트 주소는 반드시 하이퍼링크로 단다

```css
a.ext-link { color:#4338ca; font-weight:600; text-decoration:underline;
             text-underline-offset:2px; word-break:break-all; }
a.ext-link:hover { color:#312e81; }
```

- `target="_blank"` + `rel="noopener noreferrer"`를 항상 붙인다.
- `word-break: break-all`이 없으면 **긴 주소가 375px를 뚫는다.**
- 표시 글자는 `https://www.`를 뺀 짧은 형태, `href`에는 전체 주소.

### 표가 375px에서 눌리는 문제 — `table-prose`

```css
table.table-prose { min-width: 32rem; }
```

- **붙인다** — 셀에 본문 성격의 긴 글이 있거나 **열이 4개 이상**인 표.
- **붙이지 않는다** — 값이 짧은 2열 표.
- 라벨 열은 `w-28`~`w-44`. 크게 주면 32rem 안에서 본문 열이 다시 좁아진다.
- 표로 제시하는 것이 맞는 내용을 카드로 바꾸지 않는다.
- **flex 자식 안에 넣으면 부모에 `min-w-0`을 함께 준다.** 없으면 페이지가 가로로 넘친다.
  `check_html.py`는 못 잡고 **375px 실측에서만** 드러난다.

### `<sup>` `<sub>` 태그를 쓰지 않는다

브라우저가 `0.83em`으로 줄여 `text-base` 문단 안에서 12~13.5px가 된다.
**소스에 `text-sm`이 없으므로 검사기는 통과시킨다.**

- 제곱은 `&sup2;` 문자로 쓴다.
- 아래첨자는 대응 문자가 마땅치 않으므로 **문장을 고쳐 없앤다.**

### 목록은 `list-outside`만 쓴다 (`list-inside` 금지)

```html
<ol class="space-y-1 text-base font-medium text-slate-800 list-decimal list-outside pl-5">
<ul class="space-y-2 text-base text-slate-700 list-disc list-outside pl-5">
```

- 마커가 콘텐츠 박스 밖에 그려지므로 **`pl-5` 이상을 반드시 함께 준다.**
- **학습 목표 번호는 `①②③` 대신 `list-decimal` 자동 번호.**

### 글자가 있는 SVG에는 폭 천장과 가운데 정렬을 함께 준다

`class="w-full"`인 SVG는 부모 폭에 맞춰 늘어나고, `<text font-size>`도 그 비율로 함께 커진다.
천장이 없으면 **데스크톱·빔프로젝터에서 도해의 글자만 과하게 커진다.**

```html
<svg class="w-full h-auto max-w-xl mx-auto" viewBox="0 0 520 300" style="min-width:495px">
```

- 천장은 **viewBox 폭에 가까운 `max-w-*`**로 준다. 배율이 1에 가까워야 의도한 크기로 그려진다.
- **폭 천장을 주면 `mx-auto`를 반드시 함께 준다.** 없으면 좁아진 그림이 왼쪽에 붙는다.
- **높이로 묶인 그림(`max-height`, 높이 고정 부모 + `h-full`)에는 정렬을 따로 주지 않는다.**
  `preserveAspectRatio` 기본값이 이미 가운데 놓는다.
- `npm run audit:svg`가 둘 다 본다.

**바닥선도 함께 준다 — `min-width ≥ viewBox 폭 × 16 ÷ 가장 작은 font-size`.**
SVG 글자는 그리는 폭에 비례해 줄어든다. `viewBox="0 0 560 200"`인 그림을 520px로 그리면
`font-size="14"`가 **13px**로 앉는다. `min-width`와 `max-width`를 같은 값으로 묶으면
**데스크톱·빔프로젝터에서도 같은 크기**이므로, 이건 좁은 화면만의 문제가 아니다.

- **모자라면 `font-size`가 아니라 그리는 폭을 올린다.** SVG 텍스트는 줄바꿈이 없어
  글자만 키우면 고정 크기 상자를 뚫는데, 재 보기 전에는 알 수 없다.
  폭을 키우면 도형과 글자가 같은 비율로 커지므로 배치가 깨질 수 없다.
- 폭을 올리기 전에 **`overflow-x-auto` 안인지 확인한다.** 아니면 375px에서 페이지가 넘친다.
- `font-size`는 `<text>`뿐 아니라 **`<g>`에도 붙고 상속된다.** 그룹까지 봐야 한다.
- `npm run check:html`이 12px 미만은 위반, 12~16px는 「확인 필요」로 잡는다.

### 코드 요소의 한글 글꼴

요소 선택자만 쓰면 Tailwind의 Preflight에 밀린다. `body`를 붙여 특정도를 올린다.

```css
body code, body kbd, body samp, body pre,
body .font-mono { font-family: ui-monospace, …, "Pretendard Variable", …, monospace; }
```

### 코드 블록 가로 넘침

`<pre>`를 쓰는 모든 파일에 넣는다.

```css
body pre { overflow-x: auto; max-width: 100%; min-width: 0; }
```

- **`white-space: pre-wrap`으로 접지 않는다.** 코드는 들여쓰기가 의미를 가진다.
- 래퍼 `div`를 끼우지 않는다. 둥근 모서리와 flex 레이아웃이 깨진다.

### 코드 자체는 HTML에 쓰지 않는다

`.py`·`.c`는 **강의노트 옆 `code/`에 실파일로** 두고, HTML에는 마커만 둔다.
문법과 절차 → [`tools/README.md`](tools/README.md)의 「코드는 HTML에 넣지 않는다」.

```html
<pre><code class="language-c" data-src="code/조건.c#비교"></code></pre>
```

- **짧은 조각도 파일로 뺀다.**
- 소스 파일에는 코드를 **그냥 코드로** 쓴다. 이스케이프는 주입할 때 자동으로 된다.
- 복사 버튼은 `id` + `onclick` 대신 **위임 리스너 하나**를 쓴다.
- `npm run check:code`가 구문 오류와 깨진 마커를 잡는다.

### 용어의 한국어·영어 표기는 한 줄에 붙인다

```html
<p class="font-black text-lg text-sky-900">상관관계(correlation)</p>   <!-- O -->
```

핵심 정리 카드의 `<h3>` + 부제 패턴은 예외다(카드 헤더 관례).

### 짝을 이루는 개념은 나란히 배치한다

회귀↔분류처럼 **학생이 자주 뒤섞는 짝**은 `grid grid-cols-1 lg:grid-cols-2`로 놓는다.

- 두 그림은 **같은 viewBox·같은 축**으로 그린다.
- 소제목도 대구로 맞춘다.
- 나란히 놓은 뒤 **차이를 못박는 상자**를 하나 둔다. 배치만으로는 정리되지 않는다.

### 예시 데이터의 인물은 반드시 익명화한다

실제 사람 이름처럼 보이는 값은 **`학생 A`, `학생 B`**로 바꾼다.
학생·교사·환자 등 **사람을 가리키는 예시 값 전부**에 적용한다(`환자 A` 등).
학번·점수·날짜처럼 **사람을 특정하지 않는 값은 원자료 그대로** 둔다.
**표에 사람 이름이 들어가는 순간마다** 확인한다.

---

## 참고 자료는 TXT와 PDF를 함께 읽는다

**TXT만 읽고 쓰지 않는다.** TXT는 그림 안의 글자와 조판을 담지 못해,
어떤 도해·활동·예시가 있었는지가 통째로 빠진다.

1. **TXT로 용어와 수치를 확보**한다.
2. **같은 범위의 PDF 쪽을 이미지로 연다.** 그림이 무엇을 보여 주려 했는지 확인한다.
3. **의도만 가져온다.** 설명 순서 &middot; 비유 &middot; 그래픽은 **전부 새로 짠다.**
   **개념 설명의 흐름을 뒤집는 것이 학생에게 더 낫다면 뒤집는다.**

쪽 오프셋을 먼저 확정하고 적어 둔다. 확정된 값 &mdash;
**고등학교 「정보」 지도서(수업편) 1~5권 = Ⅰ~Ⅴ단원, `PDF쪽 = 교과서쪽 − 7`.**

## 시뮬레이터는 조작의 복잡도로 위치를 정한다

- **조작이 간단하면 강의노트 안에 내장**한다.
- **기능이 많거나 시뮬레이션이 복잡하면 별도 페이지**(`simulator/…`)로 뺀다.
  다만 **대단원이 끝날 때 사용자에게 보고하고 나서 제작**한다. 먼저 만들지 않는다.

## 강의노트는 자체 서술이 원칙이다

이 저장소의 강의노트는 **웹에 공개되는 독립 저작물**이다. 어떤 참고 자료를 보고 만들었든,
**읽는 사람이 그 자료를 갖고 있지 않아도 온전히 읽히도록** 쓴다.

1. **참고 자료 내부의 번호를 캡션에 쓰지 않는다.** `[표 Ⅱ-9]` 같은 번호는 그 자료 안에서만
   뜻이 통한다. 캡션은 **무엇을 담은 표인지**로 쓴다. HTML 주석에도 남기지 않는다.
2. **데이터는 원출처를 밝힌다.** 표·그림 캡션에 함께 적는다.

   | 데이터 | 원출처 |
   |---|---|
   | 세계 행복 보고서 | UN, `worldhappiness.report` |
   | 국민체력측정통계 | 문화체육관광부, `mdis.kostat.go.kr` |
   | 기상 관측 | 기상청 기상자료개방포털, `data.kma.go.kr` |
   | 한국인 혈액형 | 대한적십자사 |
   | 사과 품질 | 농촌진흥청 과수생육·품질관리시스템 |
   | 전기 자동차 | 캐글 `evs-one-electric-vehicle-dataset` |

3. **설명 문장은 직접 쓴다.** 개념 정의를 옮겨 적지 말고, 그 개념을 이해한 사람이
   학생에게 설명하듯 새로 쓴다. `fa-quote-left` 인용 블록은 **실존 인물의 발언에만** 쓰고
   누가 한 말인지 밝힌다. 일반 개념 정의에는 쓰지 않는다.
4. 용어의 한국어·영어 표기, 분류 체계, 값의 범위 같은 **사실은 그대로** 쓴다.

---

## 오개념을 심지 않는 서술

강의노트는 **틀리지 않는 것만으로 부족하다.** 맞는 문장도 학생이 틀리게 받아들이면 실패다.
아래 열하나는 실제로 반복해서 저지른 유형이므로 **파일을 쓸 때마다 확인한다.**
X / O 예시는 [사례집 2장](docs/강의노트-작성-사례집.md)에 있다.

**7 · 4 · 8번이 전체 적발의 3분의 2다. 이 셋을 먼저 본다.**

**1. 관찰한 데이터의 기울기를 인과 화법으로 읽지 않는다.**
`커질 때 ~ 늘어난다`는 한 개체가 변화한다는 뜻으로 읽힌다. **비교 화법**으로 쓴다.
직접 조작한 실험이라면 인과 화법이 정당하다. **독립·종속 변수를 지정하는 문장이 단골 구멍**이니
두 문장의 화법을 반드시 맞춘다. **집단 단위의 관계를 개인에게 옮기지 않는다**(생태학적 오류).

**2. 절편이 음수이거나 x=0이 현실에 없으면 반드시 짚는다.**
절편을 `기본값`이라 정의하면 절편이 음수인 실제 회귀식에서 학생이 모순에 부딪힌다.
**선의 높이를 맞춰 주는 숫자**로 두고, **데이터가 없는 구간까지 선을 늘였을 때 나오는 값**이라
설명한다. 외삽의 위험을 복습시킬 자리다.

**3. 정의가 순환하지 않게 쓴다.** 나란히 놓이는 개념은 **구별점을 정의문 안에** 넣는다.
카드가 정의의 첫인상을 만든다. 구별을 아래 보조 박스로 미루지 않는다.

**4. 용어를 무주석으로 처음 등장시키지 않는다.**
처음 나오는 말은 **그 자리에서** 괄호로 뜻을 단다. 뒤에서 설명한다면 그렇다고 밝힌다.
다시 나오지 않을 약어는 지우거나 **「이름까지 외울 필요는 없다」**를 붙인다.
**같은 개념을 두 파일에서 다르게 말하지 않는다.** 새 파일을 쓸 때 앞 파일의 정의를 먼저 본다.
**같은 낱말을 두 뜻으로 쓸 때는 그 자리에서 이름 충돌을 밝힌다.**
한쪽에서만 막아 두면 다른 쪽을 읽는 학생은 충돌을 모른 채 지나간다.

**5. 정의문이 뒤에 나올 예시를 배제하지 않는지 확인한다.**
요건(반드시 있어야 하는 것)과 **흔한 형태**(대개 그렇다는 것)를 섞지 않는다.
정의를 고치면 **핵심 정리 카드와 퀴즈 해설에 같은 문장이 복제되어 있으니** 함께 고친다.

**6. 파일을 잇는 예시 데이터는 값까지 동결한다.**
`앞 시간에 만든 ○○`이라고 이어 붙였으면 **표의 값이 앞 파일과 같아야 한다.**
값을 바꿔야 한다면 잇는다는 표현을 빼고 새 상황으로 제시한다. **이름도 흔들지 않는다.**

**7. 조건이 붙는 사실을 무조건으로 쓰지 않는다.**
도입부에서 `언제나`로 단순화해 두면 **같은 파일 뒤쪽의 실습이나 결론에서 반드시 모순이 난다.**
새 파일을 쓴 뒤 **도입부의 단정 문장과 실습·결론을 나란히 놓고 읽어야** 잡힌다.
방법 하나만 옳은 것처럼 쓰는 것도 같은 유형이다.
**앞 파일이 조건부로 세운 것을 뒤 파일이 무조건으로 되돌리는 일이 특히 잦다.**
앞을 제대로 배운 학생일수록 걸려 넘어진다.

**8. 절 제목·표 셀·핵심 정리 카드의 압축 문장이 본문보다 단정해지지 않게 한다.**
본문에 유보를 달아 두고 카드에서 떼면 **유보는 전달되지 않는다.** 학생이 외우는 것은 카드다.
**절 제목이 결론을 미리 정해 주는 것도 같은 유형이다.**
**파일을 다 쓴 뒤 제목·카드·표 셀·퀴즈 해설만 따로 읽어 본문과 대조한다.**

**9. 출처를 댈 수 없는 수치와 기업·제품 실명을 쓰지 않는다. 예측을 사실처럼 적지 않는다.**
숫자와 고유명사는 글에 무게를 주는 것처럼 보이지만, 근거를 댈 수 없으면 학생이 확인할 방법이
없고 해가 바뀌면 틀린 값이 된다. **연도·주체를 확실히 댈 수 없으면 일반화해서 쓴다.**
특정 기업을 비난하는 글이 되어서도 안 된다 — 요점은 **무엇이 어떻게 어긋났는지**다.
값이 필요한 표·그림에는 **「수업에서 원리를 확인하려고 지어낸 값」**이라고 밝힌다.

**10. 딜레마와 논쟁에 정답을 정해 주지 않는다. 다만 「정답이 없다」로 끝내지도 않는다.**
**각 선택이 무엇을 지키고 무엇을 내려놓는지**를 나란히 놓는다.
**많이 골랐다는 것과 옳다는 것은 다르다** — 이 구별을 빠뜨리면 다수결이 답이 된다.
**원칙을 나열만 하지 않는다.** 원칙끼리 부딪히는 짝을 하나라도 보여야 왜 어려운지가 선다.
**해결 방안에는 남는 어려움을 함께 적는다.** 「이렇게 하면 된다」로 닫지 않는다.

**11. 본문을 고치면 학습 목표의 낱말도 함께 본다.**
「제목은 네 곳이 일치해야 한다」의 확장판이다. 목표는 학생이 가장 먼저 읽는 문장이다.
**반대 방향도 본다** — 목표에 있는 말이 본문에 아예 없는 경우다.
본문을 고친 뒤 **목표·nav·핵심 정리·퀴즈 해설을 한 번에 훑는다.**
