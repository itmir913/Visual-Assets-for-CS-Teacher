# ---
# check: none
# ---
def add(a, b):
    return a + b            # 값을 «돌려준다»

result = add(3, 5)          # 돌려받은 값을 담는다
print(result)               # 8
print(add(3, 5) * 10)       # 80   돌려받은 값은 계산에 바로 쓸 수 있다
